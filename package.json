{
  "name": "x-agent",
  "version": "1.0.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "twitter-api-v2": "^1.19.0",
    "yaml": "^2.7.0",
    "zod": "^3.25.1"
  },
  "scripts": {
    "mcp": "bun run src/mcp-server.ts",
    "check": "bun run src/check-targets.ts",
    "test-auth": "bun run src/test-auth.ts",
    "auth": "bun run src/auth-setup.ts",
    "watch": "bun run src/watch.ts",
    "lint": "bunx biome check src/",
    "lint:fix": "bunx biome check --write src/"
  },
  "type": "module",
  "devDependencies": {
    "@biomejs/biome": "^2.4.10",
    "@types/bun": "^1.3.11"
  }
}
